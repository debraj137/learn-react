What is state?
---------------------------------------
State = data that changes over time
Examples:counter value, form input, login status

Why Components Re-render (INTERVIEW ANSWER)
-------------------------------------------------
Correct answer:
A component re-renders when:
Its state updates,Its props change (by reference),Its parent re-renders,Context value changes
📌 NOT because React is slow
📌 NOT because Virtual DOM is bad

API Best Practices (INTERVIEW ANSWER)
--------------------------------------------------------
✔ Fetch inside useEffect
✔ Handle loading & error
✔ Cleanup side effects
✔ Don’t fetch during render
✔ Avoid unnecessary calls
✔ Separate logic when it grows (custom hooks later)

📌 Interview-ready sentence:
-----------------------------------------------------------------
“I use useEffect for side effects like data fetching, manage loading and error states, and clean up effects to avoid memory leaks or race conditions.”

LESSON 7: Form Performance Optimization (INTERVIEW TOPIC)
--------------------------------------------------------------------
Problem:Controlled inputs re-render on every keystroke.

Solutions: ✔ Use useRef when needed, ✔ Use React Hook Form, ✔ Avoid unnecessary state
✔ Split big forms into components

📌 Interview line:
--------------------------------------------------
“For large forms, I prefer React Hook Form because it minimizes re-renders and improves performance.”

🧩 LESSON 8: Rules of Hooks (MUST MEMORIZE)
-------------------------------------------------------------------
❌ Don’t call hooks:inside loops,inside conditions,inside nested functions

✅ Call hooks:at top level,inside React components,inside custom hooks

📌 Rule:Hooks must run in the same order every render.

🧩 LESSON 9: When NOT to Use Hooks (SENIOR ANSWER)
--------------------------------------------------------------
❌ Don’t use hooks:
just to look smart
for simple values
for premature optimization

✔ Use hooks:
when state exists
when side effects exist
when logic is reusable

🧩 LESSON 3: Global State Patterns (MENTAL MODEL)
Types of state
State type	Example	Tool
Local	input, toggle	useState
Shared	filters, tabs	lifting
Global	auth, theme	Context
Server	API data	React Query